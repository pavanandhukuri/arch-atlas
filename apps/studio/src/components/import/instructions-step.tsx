'use client';

export function InstructionsStep() {
  return (
    <div className="iw-step-content">
      <h2 className="iw-step-title">Run the Repo Importer</h2>
      <p className="iw-step-subtitle">
        The import wizard turns an <code>architecture.review.yaml</code> file into a diagram. That
        file is generated by the <code>arch-atlas-import</code> CLI, which scans a folder of Git
        repositories with static analysis (and a single LLM call) to find systems and their
        connections. Run it locally first, then come back here to load the result.
      </p>

      <ol className="iw-instructions-list">
        <li>
          <strong>Install the CLI</strong>
          <p className="iw-step-hint">
            Not published to PyPI yet — install it from your local <code>arch-atlas</code> checkout.
          </p>
          <pre className="iw-code-block">
            {'# from the root of your arch-atlas checkout\npipx install ./apps/llm-importer'}
          </pre>
          <p className="iw-step-hint">
            Requires Python 3.11+. Plain <code>pip install ./apps/llm-importer</code> also works.
            After pulling new changes, re-run as{' '}
            <code>pipx install --force ./apps/llm-importer</code> to pick them up.
          </p>
        </li>
        <li>
          <strong>Run it against your repos</strong>
          <pre className="iw-code-block">
            {'cd /path/to/your/repos\narch-atlas-import propose .'}
          </pre>
          <p className="iw-step-hint">
            By default it uses a local Ollama model. To use Anthropic instead, add{' '}
            <code>--provider anthropic --model claude-opus-4-8</code> (requires{' '}
            <code>ANTHROPIC_API_KEY</code>).
          </p>
        </li>
        <li>
          <strong>Locate the review file</strong>
          <p className="iw-step-hint">
            The command writes <code>architecture.review.yaml</code> to{' '}
            <code>PROJECTS_DIR/.arch-atlas/</code>. That&apos;s the file you&apos;ll upload in the
            next step.
          </p>
        </li>
        <li>
          <strong>Continue below</strong>
          <p className="iw-step-hint">
            Click &ldquo;Next&rdquo; to load the review file and start classifying elements and
            reviewing connections.
          </p>
        </li>
      </ol>
    </div>
  );
}
