{
  "name": "@arch-atlas/dsl",
  "version": "0.1.0",
  "description": "Plain-text DSL for describing architecture models — parse and serialize ArchitectureModel",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/ test/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist coverage"
  },
  "peerDependencies": {
    "@arch-atlas/core-model": "workspace:*"
  },
  "devDependencies": {
    "@arch-atlas/core-model": "workspace:*",
    "@types/node": "^20.11.0",
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  }
}
