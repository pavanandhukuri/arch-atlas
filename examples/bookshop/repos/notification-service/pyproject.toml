[project]
name = "notification-service"
version = "1.0.0"
description = "Sends order confirmation emails"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.29",
  "aiokafka>=0.10",
  "httpx>=0.27",
  "sendgrid>=6.11",
]
